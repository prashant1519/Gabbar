// // let nam="ankush"

// // for(var i=nam.length-1;i>=0;i--){
// // console.log(nam[i]);
// // }

// var arr = "masai";
// var left = 0;
// var right = arr.length - 1;

// while (left < right) {
//   swap(arr, left, right);
//   left++;
//   right--;
// }

// function swap() {
//   var temp = arr[left];
//   arr[left] = arr[right];
//   arr[right] = temp;
// }

// console.log(arr);

let arr = [1, 2, 3, 4, 5];

for (var i = 0; i < arr.length; i++) {
  if (arr[i]) {
    arr[i] = 1;
  }
}
console.log(arr);
